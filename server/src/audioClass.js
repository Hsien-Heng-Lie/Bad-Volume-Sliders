class Audio {
  constructor(id, title, artist, path, file) {
    this.id = id,
    this.title = title;
    this.artist = artist;
    this.path = path;
    this.file = file;
  }
}

module.exports = Audio;